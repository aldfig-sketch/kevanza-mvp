// Kept as a compatibility shim for pages that still import Header.
// AppLayout owns the single rendered Navbar for authenticated pages.
export function Header() {
  return null
}

export default Header
